import Home from '@/components/home';
import { render, screen } from '@testing-library/react';

describe('Home', () => {
  it('1 + 1 = 2', () => {
    const a = 1;
    const b = 1;

    expect(a + b).toBe(2);
  });
});
